<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/relative"
    android:orientation="vertical"
    tools:context="arvapp.navigation.BluetoothActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/mount"
        android:scaleType="fitXY"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="@string/ble_info_hint"
        android:id="@+id/ble_info"
        android:layout_marginTop="30sp"
        android:textColor="@android:color/white"
        android:textAlignment="center" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="55sp"
            android:layout_centerHorizontal="true"
            android:text="@string/ble_butt_hint"
            android:id="@+id/ble_enable_butt"
            android:background="@drawable/custom_button_scan"
            android:textColor="#ffffff"
            android:textSize="15sp" />

</RelativeLayout>
